info:
  name: 2. Logout Usuario
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/auth/logout
  body:
    type: json
    data: |-
      {
        "refreshToken":"39c1714a61f63de1541ed74c19d061ccd53158908edb9797ea67abed811d763d25d0ceb55727e952e3f236f58b1d3398"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  # POST /auth/logout

  ## Descripción
  Cierra la sesión revocando el `refreshToken` recibido.

  ## Autenticación
  No requiere `Authorization` header.

  ## Content-Type
  - `application/json`

  ## Body (obligatorio)
  - `refreshToken`: token de refresco que se desea revocar.

  ## Ejemplo (JSON)

  ```json
  {
    "refreshToken": "<opaque_refresh_token>"
  }
  ```

  ## Respuesta Exitosa
  `200 OK`

  ```json
  {
    "message": "Session closed successfully"
  }
  ```

  ## Comportamiento relevante
  - El backend revoca el refresh token por hash.
  - Si el token ya estaba revocado o no existe, el endpoint mantiene respuesta de éxito para no filtrar estado de sesión.

  ## Errores comunes
  - Body sin `refreshToken`: `500 Internal Server Error` (validación de backend).
  - Error inesperado: `500 Internal Server Error`.
